'''
Slicing tuples
'''

if __name__ == "__main__":
    # Tuple can  have multiple types
    t = (1, 2, 3, 4)
    # Return all elements
    print(t[:])
    # Return for index 1
    print(t[1:])

    # Return only last element. Index of last element is -1
    print(t[-1:])
